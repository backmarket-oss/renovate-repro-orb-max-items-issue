version: 2.1

orbs:
  # This version is more than 10 releases behind the latest one.
  slack: circleci/slack@6.1.3
  # This version is less than 10 releases behind the latest one.
  github: circleci/github-cli@2.2.0
